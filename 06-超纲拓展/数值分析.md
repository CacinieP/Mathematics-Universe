# 数值分析——计算机时代的数学

> 纯数学回答"能不能"，数值分析回答"怎么算"。

## 一、误差分析

### 1.1 误差来源

| 类型 | 来源 | 例子 |
|------|------|------|
| 模型误差 | 数学模型本身的近似 | 用线性模型近似非线性 |
| 截断误差 | 用有限代替无限 | Taylor展开截断 |
| 舍入误差 | 计算机有限精度 | 浮点运算 |

### 1.2 浮点数

IEEE 754双精度：$(-1)^s \times 1.f \times 2^{e-1023}$

- 有效位数约15-16位十进制
- 机器精度 $\epsilon_{mach} \approx 2.2 \times 10^{-16}$

**经典问题**：$\sin(10^{20})$ 用不同算法可能得到不同结果——有效数字全部丢失。

## 二、方程求根

### 2.1 二分法

若 $f$ 在 $[a,b]$ 连续，$f(a)f(b) < 0$，则区间内有根。

收敛速度：每步缩小一半，$n$ 步后误差 $\leq \frac{b-a}{2^n}$。

### 2.2 Newton法

$$x_{n+1} = x_n - \frac{f(x_n)}{f'(x_n)}$$

**几何意义**：在 $x_n$ 处作切线，切线与 $x$ 轴的交点就是 $x_{n+1}$。

**收敛性**（局部收敛定理）：若 $f$ 二次连续可微，$f(\alpha)=0$，$f'(\alpha)\neq 0$，则Newton法二阶收敛：
$$|x_{n+1}-\alpha| \leq C|x_n-\alpha|^2$$

**与高等数学的联系**：Newton法的本质是**线性化**——用切线（一阶Taylor）近似函数。

### 2.3 案例：求 $\sqrt{2}$

$f(x) = x^2 - 2$，$f'(x) = 2x$

迭代：$x_{n+1} = \frac{x_n}{2} + \frac{1}{x_n}$（这就是Newton法）

从 $x_0 = 1$：
- $x_1 = 1.5$
- $x_2 = 1.4167$
- $x_3 = 1.414216...$
- $x_4 = 1.414213562...$

4步就精确到10位小数——**二阶收敛的威力**。

## 三、数值线性代数

### 3.1 解线性方程组

**Gauss消元法**：时间复杂度 $O(n^3)$

**LU分解**：$A = LU$，一次分解，多次求解。

**迭代法**（大型稀疏矩阵）：
- Jacobi迭代：$x_i^{(k+1)} = \frac{1}{a_{ii}}\left(b_i - \sum_{j\neq i}a_{ij}x_j^{(k)}\right)$
- Gauss-Seidel迭代：利用最新分量
- 共轭梯度法：正定矩阵的最优迭代法

**收敛条件**：迭代矩阵的谱半径 $\rho(B) < 1$。

**与线性代数的联系**：谱半径 = 特征值的最大模。线代的特征值理论直接决定了数值算法的收敛性。

### 3.2 最小二乘法

**问题**：$Ax = b$ 无解（超定方程组），找最优近似。

**解**：$x^* = (A^TA)^{-1}A^Tb$（正规方程）

或用QR分解：$A = QR$，$x^* = R^{-1}Q^Tb$

**概率论联系**：当误差服从正态分布时，最小二乘估计 = 最大似然估计。

### 3.3 特征值计算

**幂迭代法**：
$$\mathbf{v}_{k+1} = \frac{A\mathbf{v}_k}{\|A\mathbf{v}_k\|}$$

收敛到最大特征值对应的特征向量。

**应用**：这就是PageRank的核心算法。

## 四、数值积分

### 4.1 Newton-Cotes公式

用多项式插值近似被积函数。

**梯形公式**（一次多项式）：
$$\int_a^b f(x)dx \approx \frac{b-a}{2}[f(a)+f(b)]$$

**Simpson公式**（二次多项式）：
$$\int_a^b f(x)dx \approx \frac{b-a}{6}[f(a)+4f\left(\frac{a+b}{2}\right)+f(b)]$$

### 4.2 Gauss积分

在最优节点上求值，$n$ 个节点达到 $2n-1$ 阶精度。

**与正交多项式的联系**：Gauss积分节点 = Legendre多项式的零点。

**与概率论的联系**：Gauss-Hermite积分 = 对 $e^{-x^2}$ 加权的积分 → 正态分布的数值积分。

## 五、微分方程数值解

### 5.1 Euler法

$$y_{n+1} = y_n + hf(x_n, y_n)$$

一阶精度，$O(h)$。

### 5.2 Runge-Kutta法（RK4）

$$y_{n+1} = y_n + \frac{h}{6}(k_1 + 2k_2 + 2k_3 + k_4)$$

四阶精度，$O(h^4)$。

RK4的本质：用4次函数求值来匹配Taylor展开到4阶。

━━━ 联系网络 ━━━
⬆ 上游: [[高等数学全貌]]（微分·积分·微分方程），[[线性代数全貌]]（矩阵运算）
⬇ 下游: 科学计算、工程仿真
↔ 横联: 计算机科学（算法设计·复杂度分析）
🔗 跨域: 物理仿真（CFD·FEA），金融（期权定价的数值解），机器学习（优化算法）
━━━━━━━━━━━━━━
